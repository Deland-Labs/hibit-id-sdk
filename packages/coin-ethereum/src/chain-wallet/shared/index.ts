/**
 * Shared services used by Ethereum asset handlers.
 *
 * These services provide common functionality and resource management
 * that is shared across different asset type handlers.
 */
export { ConnectionManager } from './connection-manager';
